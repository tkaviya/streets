<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="
            http://www.springframework.org/schema/beans
            http://www.springframework.org/schema/beans/spring-beans.xsd
            http://www.springframework.org/schema/security
            http://www.springframework.org/schema/security/spring-security.xsd">

    <beans:import resource="classpath*:engine-context.xml" />

    <http pattern="/login" security="none" />
    <http pattern="/loginFailed" security="none" />
    <http pattern="/logout" security="none" />
    <http pattern="/logoutSuccess" security="none" />

    <http auto-config="true">
        <intercept-url pattern="/**"  access="ROLE_USER"/>

        <form-login login-page="/login" default-target-url="/loginSuccess"
                    authentication-failure-url="/loginFailed"
                    login-processing-url="/doLogin"
                    always-use-default-target="true" />

        <!--<custom-filter before="LOGOUT_FILTER" ref="logoutBean"/>-->

        <logout logout-url="/doLogout" logout-success-url="/logoutSuccess"
                delete-cookies="JSESSIONID" invalidate-session="true" />
    </http>

    <!--<beans:bean id="logoutBean" class="net.blaklizt.streets.web.LogoutHandler"/>-->

    <beans:bean id="authentication" class="net.blaklizt.streets.engine.authentication.StreetAuthenticator" />

    <authentication-manager>
        <authentication-provider user-service-ref="authentication">
            <password-encoder ref="authentication" />
        </authentication-provider>
    </authentication-manager>

</beans:beans>